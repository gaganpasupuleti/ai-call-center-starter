# One-shot private speech test runner (no public domain).
# Start command overridden to the layered 4E.2 battery.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node scripts/run-phase4e2-layered-battery.mjs"
restartPolicyType = "NEVER"
